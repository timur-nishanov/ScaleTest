# Разбор макетов Figma

Файл: `Yandex-Scale-2026` (key `hSsvJ58i9BQPwmZlZUnBuv`), секция **Screens** (`2334:3040`).
Все экраны — 3840×2160. Ниже — инвентаризация node id (для design context на этапе вёрстки) и зафиксированные расхождения с ТЗ.

## Экраны

| Экран | Node ID | Заметки |
|---|---|---|
| Главный экран (заставка) | `2334:3570` | Логотип 671×95 @ (90,90); заголовок/сабтайтл; кнопка «Начать» 460×136; 3 факт-блока с декоративными фигурами; ряд тегов 11943×112 @ y=1958 — шире экрана, бегущая строка |
| Как будем собирать бандл | `2334:3041` | 2 карточки 1180×1428 с иллюстрациями; у «Выбрать из готового» чип «Монета за конкурс» 366×84; кнопка «Назад» 267×128 |
| — стейты | секция `2340:38935` | Ховер `2334:3304`, нажатие из ховера `2334:3437`, нажатие тач `2334:3172` |
| Собрать самому (выбор задачи) | `2334:4845` | Колесо из 10 карточек (см. ниже); кнопка «Случайный выбор» 593×128 с иконкой dice-5; стрелки-слайдер 2×133×133 @ (1768,1937) |
| — стейты | секция `2340:38936` | Ховер `2334:5049`, нажатие из ховера `2334:5459`, нажатие тач `2334:5254` |
| Выбрать из готового (выбор задачи) | `2334:5831` | Колесо из 6 карточек; **кнопка случайного выбора присутствует** (в ТЗ её нет) |
| Собрать самому. Задача (дефолт) | `2334:4377` | Палитра 13 плиток 248×226; 3 слота 648×300 со стрелками-коннекторами; подсказка с иконкой «рука»; таймер 297×128; CTA «Очистить» 336×136 + «Проверить связку» 521×136 |
| — стейты задачи | | Ховер `2334:3760`, нажатие после ховера `2334:3864`, нажатие тач `2334:4479`, нажали на сервис `2334:3658`, тянем после тапа `2334:3968`, тянем без тапа `2346:1715`, дропнули сервис `2334:4071`, дропнули все `2334:4173`, красный таймер `2334:4275` |
| Выбери готовое. Задача (дефолт) | `2334:4582` | 3 карточки бандлов 887×958 в ряд, описания видны сразу, подпись «Выбери подходящий бандл»; таймер; **панели описания и кнопки «Подтвердить» нет** |
| — стейты | | Ховер `2334:4647`, нажатие при ховере `2334:4713`, нажатие тач `2334:4779` |

## Секции компонентов (без поворотов)

| Секция | Node ID | Состав |
|---|---|---|
| Карточки. Собрать самому | `2334:5664` | 10 карточек 790×1060: Сбор и анализ, Регулярный ETL, Аналитика, Хранилище и пакетная, Подготовка больших данных, Хранилище пользовательских, Приём и обработка, Полнотекстовый поиск, Транзакционное ядро, Каталог и корзина |
| Карточки. Выбрать из готового | `2334:5976` | 6 карточек: Сбор и анализ логов, Аналитическое хранилище, Онлайн-дашборд, Бэкенд, Платформа для обработки, Поиск |
| Теги | `2334:6079` → `2334:6080` | Ряд всех тегов |
| Popup states | `2334:6135` | Результаты как полноэкранные фреймы: build «Почти» (1/2/3 красных, 2-в-ряду), «В точку» (+2-в-ряду), «Время вышло» (+2-в-ряд); ready «Отличный выбор», «Почти угадал», «Мимо», «Время вышло» (все ×2: 3-в-ряду и 2-в-ряду) |

Структура карточки задачи: 790×1060, внутренний контент 630×880 (паддинг 80), сверху тип-лейбл (напр. «Логи / наблюдаемость») + заголовок + описание, снизу иллюстрация связки (~329×220).

Попап результата (пример `2334:6136`): затемнение поверх экрана задачи; модалка 1560×1760 (иллюстрация 396×320, заголовок, текст, блок сравнения 1460×570 с двумя рядами чипов 1340×200, кнопка «Выбрать другую задачу» 629×136); в ветке А справа промо-карточка монеты 742×850 («Хочешь монету для конкурса?…» + кнопка «Перейти»).

## Колесо карточек (геометрия)

Из bounding box повёрнутых карточек в `2334:4845`: шаг между соседними карточками **15°** (0°, ±15°, ±30°, ±45°, ±60°, ±75°), центральная карточка 790×1060 @ (1526,593), пивот вращения далеко под экраном (радиус ~4000px — уточним при вёрстке). Стрелки — шаг на карточку, драг — свободное вращение с инерцией и снапом.

## Аннотации в файле

- `2346:27773` (большая, над колесом) — текст приведён заказчиком: интро-прокрутка всех карточек от начала до конца (реф lionsgoodnews), затем скролл влево-вправо; рефы листания: spencergabor.work, ponpon-mania.com/chapters.
- `2334:38810`, `2334:38815` (короткие, над экранами задачи) — текст пока не снят (лимит Figma MCP); забрать через design context при вёрстке. TODO(design-sync)

## Палитра сервисов (13) и облако тегов (18)

Палитра: Managed PostgreSQL, Managed MySQL, Managed ClickHouse, Managed Valkey, Yandex StoreDoc, Managed Kafka, Managed Greenplum, Managed OpenSearch, Managed Airflow, Managed Spark, YTsaurus, YDB, DataLens.
Теги добавляют: Trino, Data Transfer, MPP Analytics Engine for PostgreSQL*, Sharded PostgreSQL, WebSQL, DataLens Platform.

\* Иконка плитки «Managed Greenplum» в макете — инстанс «MPP Analytics Engine for PostgreSQL» (ребрендинг); в тегах — полное имя «Yandex MPP Analytics Engine for PostgreSQL». В коде это один сервис `greenplum` с разными подписями (name/tagName).

## Расхождения ТЗ ↔ макеты (макеты — истина)

| # | ТЗ | Макет | Принятый дефолт |
|---|---|---|---|
| 1 | Таймер 60 с | Таймер 00:45 | 45 с (`TIMER_SECONDS`) |
| 2 | 12 задач «собери сам» | 10 карточек | 10 |
| 3 | Палитра = все сервисы облака (18+) | 13 плиток | 13 |
| 4 | Плитки без подписей (спойлер) | Подписи на плитках есть | С подписями |
| 5 | В ветке Б нет случайного выбора | Кнопка есть (`2334:5831`) | Есть (флаг `randomInReadyMode`) |
| 6 | Экран бандлов: две колонки, панель описания, «Подтвердить выбор» | 3 карточки с описаниями, тап по карточке = выбор | Тап = ответ сразу |
| 7 | Результат — отдельный экран, кнопки «Вернуться в начало» + «Выбрать из готового» | Попап поверх задачи, кнопка «Выбрать другую задачу» + промо-карточка «Перейти» | Как в макете |
| 8 | Эмодзи-реакции в результатах | Иллюстрации (эмодзи «не важны» — переписка в ТЗ) | Иллюстрации |
| 9 | Факт-ротатор (смена раз в ~4 с) | 3 статичных факт-блока | Статичные |
| 10 | «Проверяется и последовательность и набор» vs «набор == эталон» | В попапе подсветка по-слотно со стрелками | Порядок учитывается (флаг `orderMatters`) |

## Открытые вопросы (нужно подтверждение)

1. Порядок сервисов в слотах учитывается? (дефолт: да)
2. Интро колеса: пробег до конца и возврат к первой? Начальная карточка — первая или центральная из макета? (дефолт: пробег и возврат к первой)
3. Попап «Вы ещё здесь?» — будет дизайн? (в макетах нет)
4. В ветке Б случайный выбор оставляем? (дефолт: да, по макету)
5. Тап по бандлу = сразу ответ, без подтверждения? (дефолт: да, по макету)
6. Промо-карточка монеты — во всех исходах ветки А, включая «В точку»? (дефолт: да)
7. Таймер 45 с — подтвердить (в ТЗ было 60).
